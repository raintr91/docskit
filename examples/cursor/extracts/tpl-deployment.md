# Deployment stub — DEP-* (optional)

Path: `architecture/07-deployment/`  
Only write a real diagram when **placement** matters. Default = short stub.

## Sections

`# DEP-* — title` · 2–5 lines prose · optional `## Diagram` · `## Notes` (no secrets)

## Stub example

```markdown
# DEP-local — Local developer layout

status: stub

Local WSL / Docker-compose style layout for platform bases.
Prod/staging topology: TBD (do not invent).

## Notes

- No secrets; no local-machine handbook in this repo
```
