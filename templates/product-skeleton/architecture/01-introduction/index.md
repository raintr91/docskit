# 01 — Introduction

status: active

## Purpose

Goals and scope of this **docs hub** architecture narrative (*why* we document) — not a product marketing intro.

## Goals

1. **Navigate** — arc42 chapters as TOC so humans and AI skills land on the right layer.
2. **Describe** — C4 views (`LND`/`CTX`/`CTR`) + product `CMP-*` without duplicating Code.
3. **Curate** — only high-value runtime journeys (`FLOW-*`), not every agile story.

## Audience

| Reader | Use |
|--------|-----|
| Platform / product engineers | Context → containers → component → journey |
| AI skills (`/architecture`, `/journey`, …) | Chapter + ID routing |
| New joiners | Start §03 + `FLOW-login`; deepen via §05 / product |

## Out of scope

- Feature Code (`W-*`/`API-*`) and grill/codegen detail → `product/`
- Day-to-day toolchain how-to → `platform/`
- Full E2E/`TC-*` plans → `base-tests`

## See also

- People entry: [Start now](/platform/guide/start-now)
- Tree: [System doc structure](/platform/guide/SYSTEM-DOC-STRUCTURE)
- [03 Context](/architecture/03-context/)
